{
  "name": "asiancargocare",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p .vercel/output/static && cp -rv index.html assets public favicon.ico og-image.png placeholder.svg robots.txt sitemap.xml scripts lovable-uploads .vercel/output/static/ 2>&1 && echo 'BUILD_DONE_FILES:' && ls -la .vercel/output/static/ && ls -la .vercel/output/static/assets/ 2>&1"
  }
}
